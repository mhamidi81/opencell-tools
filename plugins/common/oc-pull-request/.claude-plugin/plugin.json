{
  "name": "oc-pull-request",
  "description": "Push changes and create a pull request for a JIRA ticket with automatic squash and PR creation, then — on opencell-portal only — record the AI usage on the ticket by invoking /oc-fe-calculate-ai-use on the post-squash commit (idempotent; skipped for every other repository)",
  "version": "1.1.0"
}
